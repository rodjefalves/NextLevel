server:
  port: 8081
  servlet:
    context-path: /api
#logging:
#  level:
#    liquibase: INFO
spring:
  application:
    name: auth
  datasource:
    driverClassName: org.h2.Driver
    password: 'nlpass'
    url: jdbc:h2:mem:nlapi
    username: nextlevel
    h2:
      console:
        enabled: true
        path: /h2-console
    jpa:
      database-platform: org.hibernate.dialect.H2Dialect
      hibernate:
        ddl-auto: create-drop
    security:
      filter:
        order: 10
      jwt:
        chave-assinatura: "Ym9tIGRpYSBxdWUgbyBzb2wgamEgbmFzY2V1"
        expiracao: 30
#  liquibase:
#    change-log: classpath:/db/changelog-master.xml
